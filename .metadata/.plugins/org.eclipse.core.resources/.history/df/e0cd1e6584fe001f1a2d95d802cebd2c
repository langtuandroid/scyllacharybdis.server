package com.pikitus.games.chess.handlers;

import com.pikitus.games.chess.ChessBoard;
import com.pikitus.games.chess.SFSChess;
import com.smartfoxserver.v2.entities.User;
import com.smartfoxserver.v2.entities.data.ISFSObject;
import com.smartfoxserver.v2.extensions.BaseClientRequestHandler;

public class BoardHandler extends BaseClientRequestHandler 
{
	ChessBoard mGameBoard;
	
	public BoardHandler(ChessBoard board)
	{
		mGameBoard = board;
	}
	
	@Override
	public void handleClientRequest(User user, ISFSObject params)
	{
		SFSChess gameExt = (SFSChess) getParentExtension();
		sendBoard( gameExt );
	}
	
	public void sendValidMoves() 
	{
		send("CHESS_BOARD", mGameBoard.getBoardArray(), getParentRoom().getUserList());		
	}
}
